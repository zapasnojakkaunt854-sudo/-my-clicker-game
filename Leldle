q
2. **`sw.js` (Service Worker)** — скрипт, который помогает игре сохранять файлы в память телефона, чтобы она мгновенно загружалась.
3. Подключить `manifest.json` в твоем файле `index.html` внутри тега `<head>` вот такой строчкой:
   ```html
   <link rel="manifest" href="manifest.json">
